---
source_url: https://www.youtube.com/watch?v=1OLrT3dEzhA
source_type: youtube
source_platform: youtube.com
title: "Building AI Agents from Scratch | Full Course"
author: The Neural Maze
video_id: 1OLrT3dEzhA
captured_at: 2026-04-22
duration_min: 102
status: raw
---

# Raw transcript

[00:00:03] hey everyone so in this video I'm going to make a compilation of my previous videos on agentic design patterns so if you have watched those before then this will feel really familiar because it's basically a compilation the reason why I've done this video is that I thought it would make sense to combine my four lessons about theed design patterns into one single course because I've seen that there's a lot of traction a lot of interest in the ml Community about the series of videos so I thought okay
[00:00:34] the series of videos so I thought okay we could make some kind of comprehensive open-source free uh course so that's the reason behind this video okay and for those of you who are new to the neuromas and to my content just to let you know that I believe this course is a very good way to get started in agentic ki the reason is that we are not going to rely on any framework we are basically going to implement from scratch just using python and Gro as the llm provider uh we are going to implement from scratch the four agentic patterns that
[00:01:06] scratch the four agentic patterns that is we are going to implement the reflection pattern a reflection agent we are going to implement tools and the tool decorator and also a tool agent we going to implement the react pattern this is the most common commonly used um pattern out there and we are going to implement of course a react agent and finally we are going to implement uh multi-agent framework ourselves for from scratch that is going to work uh pretty similar to crew Ai and also to Apache flow and yeah that's basically what we
[00:01:37] flow and yeah that's basically what we are going to build throughout this course is a structor as I was saying in four modules the first module is going to be about the reflection pattern the second about tools the third about the planning pattern and the and the fourth one about the multi-agent pattern all right so I think that's everything for this quick introduction let me know what you think in the comments of this video and I also drop a link to my substack block where I have created complimentary lessons to each one of these modules or videos uh where I'm explaining with uh
[00:02:09] videos uh where I'm explaining with uh written words what I'm trying to explain here in the videos and you will also have coded snapits Etc and of course you will also have the GitHub repository if you want to check it out all right so so yeah that's everything uh let me know what you think and also let me know if you are interested in any additional topic for me to cover in this YouTube channel channel uh but yeah happy learning and I hope you enjoy this course see you on the other other [Music]
[00:02:52] side so yeah I'm going to show you the project that I've been working on for the last couple of days it's called identic patterns and it's a from scratch implementation of the four identic patterns as defined by Deep learning.ai if you go to the r me in the repository which by the way I'm going to leave in the description of the video you will see that there are four agentic patterns the reflection pattern the tool use pattern the planning
[00:03:22] the tool use pattern the planning pattern and the multi-agent pattern today we are going to start with a reflection pattern we are going to implement from scratch this pattern but in the next videos because this is going to be a video series in the next videos we are going to implement the remaining patterns that is the tool use the planning and the multi-agent pattern let me explain to you uh very simply what uh what's the basics of this reflection pattern so as we were saying in today's
[00:03:53] pattern so as we were saying in today's video we are going to focus on this pattern right here the reflection pattern which is the most basic but at the same time the results are quite interesting in terms of the llm results so this diagram showcases the inner loop that we're going to find in this reflection pattern so you can see there are two main blocks the generate block right here and the reflect block and what we are going to do I mean the workflow of this pattern is the
[00:04:24] the workflow of this pattern is the following so suppose uh the user provides a prompt like for example generate an essay about something let's say Baldi for example the composer okay so this is the user prompt all right this user prompt is going to be received by the llm in this case I'm going to use uh groc but yeah we are going to this block is going to receive
[00:04:55] going to this block is going to receive this prompt and what it's going to do is basically generate an essay about Baldi I mean this is uh like a very zero shot uh prompting style it's um really simple and the essay uh is this uh text file that you can see right here this icon portrays the the result of this generate block after that the result is going to be sent into the reflect block and this reflect block is going to
[00:05:26] and this reflect block is going to provide some critique some feedback to the generate block you can see in here this icon right here with DM is going to provide some suggestions some modifications Etc to the original content after that the result of this critique is going to be sent again to the generate block and now the generate block is going to generate another version of the txt of this essay but taking into consideration all the
[00:05:57] but taking into consideration all the critique all the feedback that a reflect block sent to it okay so now it's going to generate a V2 of this txt okay and the loop will continue over and over generally you can uh stop the loop with two uh using two ways uh the first way is to use an nend number of steps like for example keep it rating for 10 steps and then stop the loop and another way is to Define some
[00:06:30] loop and another way is to Define some stop keywords like for example suppose that in the reflect block we say that um in the system prompt on of this llm we could say something like um when you think that the result or the essay the the essay about B about Baldi is good enough you can generate uh this string this stop string for example or you can generate something like okay
[00:07:02] or you can generate something like okay and when during the iteration we receive any of these stop strings we will stop the iteration basically and the the loop will finish okay so uh this is I know this is very theoretical but I want you to really understand the the inner workings of this pattern because uh I mean you can possibly apply this simple pattern to many of your llm calls and I can assure you that it will probably improve the results by a
[00:07:33] improve the results by a lot okay so now that we have all of this covered all the theory covered I think it's time to go to the code so the first thing you will need to do is to Simply clone this uh repository and we will be working with two main folders the first folder let me show you in vs code so the first folder that you're going to work with is the notebooks folder these notebooks contains just a reflection pattern at this time but as time goes by
[00:08:03] pattern at this time but as time goes by you will see uh more notebooks inside this folder as I continue to develop this project and then we will also work with this uh foldering here uh the reflection pattern folder inside this directory which contains like a proper implementation in implementation in Python but we will start with the notebook where I will give you some easy to understand implementation of the of this pattern and after that we will move
[00:08:33] this pattern and after that we will move into the python implementation so once you have the repository installed you can simply start uh jupyter lab jupyter notebook and work with me in this uh notebook so the reflection pattern as we said before um it's a pattern that allows the D llm to reflect and critique its outputs as you can see we are using the same diagram for the reflection pattern and it follows the next steps that we already know so dlm the first step D llm generates a
[00:09:04] the first step D llm generates a candidate output the Second Step which is this three step here in the diagram the llm reflects on the previous output and finally the llm modifies the original output based on the reflections and another iteration begins so we already know that uh so now it's time to to to implement okay so we will start with the first generation step like an easy to go implementation of this steps so that you can really appreciate the way that the
[00:09:35] can really appreciate the way that the output is going to be refined and it's going to be improved in the different steps so in this generation step we are going to ask the llm to generate an implementation of a very common a very famous sorting algorithm which is called the merge sort okay we'll be using roock as I told you before and in particular we will use the Lama 3 70 billion model okay so let's uh import relevant uh
[00:10:06] okay so let's uh import relevant uh packages and clients this case the important one is the the client okay by the way uh you can see here that I'm loading all the environment variables uh let me remind you that you will need the gro API key I'm not going to get into the M but as you can imagine you will need a Gro API key defined in this environment variables environment variables file for everything to work okay so let's begin then and let's generate a
[00:10:39] let's begin then and let's generate a system prompt in this case we are going to tell the generate block to behave as a python programmer task with generating high quality python code your task is to generate the best content possible for the user's request if the user provide critique respond with a revised version of your previous attempt Okay so this is going to be the the system prompt for this block over here okay for the
[00:11:09] generate generate block right now we are going to continue and we are going to add another prompt in this case a use a prompt into the generation chat history into the history of messages for the for the generate block and in this case the content is going to be the user content uh generate the python implementation of the merge short short algorithm okay so finally we are just going to uh generate the the python
[00:11:40] going to uh generate the the python implementation and we are going to append the result as an assistant role because this is the response from the llm uh to the chat history as well okay now simply display the markdown to see the first uh step that we are going to find so as as you can see uh here's a python implementation of the mer short algorithm and you can see that it makes uh sense more or less yeah I think so yeah it makes sense we have two
[00:12:11] yeah it makes sense we have two functions defined the merge sord function and also the merge function and we have some example usage which is really cool as well now let's get into the reflection step you remember that the reflection step is in here and it will reflect on the code generated by the previous step okay it will reflect it will provide feedback to this step to this content sorry to this
[00:12:41] this step to this content sorry to this content in here okay as you can see uh there's a picture of andar pathy I thought it would be funny for the llm to behave as Andre car pathy when when it provides feedback to the generate block so that's what I did um as you can see the content uh well the the system prompt for this block for the generation block block states that you are aathy and experienced computer scientist and you are tasked with generating critique and recommendations for the users code so we are going to uh put the system prompt
[00:13:11] are going to uh put the system prompt into the reflection chat history just uh notice in here that we have to use two different um histories one for the generation block and another for the reflection reflection block which are represented by two python lists by the way so now let's put the reflection chat history and let's generate a critique to the python code let's generate this uh feedback okay so let's see what Kathy has to has to say okay as you can see there are a lot
[00:13:42] say okay as you can see there are a lot of things uh in here uh so code organization and comments your code is where organized blah blah blah but consider adding a blank line between the function signature okay some Minor Details uh naming conventions which is a good remark uh recursion merch function um some complaints about performance uh the example usage which seems to be okay yeah but it says that um we should add more test cases to
[00:14:14] um we should add more test cases to demonstrated repass of your implementation which sounds sensible and yeah a minor nitpic and here we have a very beautiful um implementation or revised implementation of the same function you can see there are some dock string string and yeah doctrines in the two functions and as well like some sample usage this is the previous one I think and more additional test cases okay so now let's add um these system prompt into the
[00:14:45] add um these system prompt into the generation chat generation chat history and as you can see we have ended uh the first step okay this first step and now we will start again in here we will start again another step uh where the generation block is going to use the feedback provided by the reflection block so that's really easy to do we just need to pass the updated generation chat history into the generation block
[00:15:16] chat history into the generation block and simply display the result okay so as you can see um the generation block has uh generated some Revised Code it implements the measured again but with do strings and the merch again with dock strings and it provides some some additional tests and example usage and now the iteration will continue I mean we could create another section in the notebook where we will
[00:15:47] section in the notebook where we will generate uh another generation step two where we will uh reflect on the output from the generation block once again but the loop will continue over and over so what I did is to implement a python class that reflects in a much better way this reflection this reflection pattern and as you can see here we are importing this reflection agent from the
[00:16:17] importing this reflection agent from the library from uh agentic patterns this is really easy to do I mean you just need to go to go to to vs code or to py turn and install the pack package using poetry in this case I'm using poetry for all the dependency management and all the the packaging of the Python package and once this is installed you will be able to access the classes that I will be defining but let me show you the class that um I implemented for
[00:16:48] the class that um I implemented for this it's called the reflection agent as we saw and it's really easy to understand uh I think at least but you can leave your impressions in the comments but it just defines one generate method that simply generates the the code the Met code if we if we keep using the example and then it also implements a reflect method which reflects on the Generation by generating
[00:17:18] reflects on the Generation by generating a critique or feedback for the generation block as we saw in the notebook but this is like a proper implementation in Python it also uses San colorama as you will see to create beautiful uh login colors uh when we print the outputs I think it's uh I I think it improves like the readability of the of the login and finally the run method which is the main entry point of this class where you will pass the generation system prompt as we defined in the
[00:17:49] system prompt as we defined in the notebook the reflection system prompt a user prompt the maximum number of steps for the iteration to run and a verbosity level level and it will basically Implement everything that we have defined in the notebook so it will generate the generation history list and also the reflection history list and it will start iterating over the number of steps this fancy step tracker is just a
[00:18:19] steps this fancy step tracker is just a helper tool for all the colorama stuff for printing visuals and then we will generate sorry uh we will generate the output based on generation history we will update histories and we will reflect and critique the generation and the loop will keep going and going until the iteration the iteration stops okay so let's see this in action let's simply uh import the reflection agent from agentic patterns
[00:18:50] reflection agent from agentic patterns let's instantiate the agent and let's define the basic prompts that we have defined previously but let's redefine them for for clarity so first of all the generation system prompt we are going to we just copy and paste it the previous prompt then the reflection system prompt we just copy and paste it again and finally The Prompt or the user prompt if you want to be more precise on the naming which is the python implementation of the mer short
[00:19:20] implementation of the mer short algorithm and now we just going to run this agent okay and as you will see it's going to start iterating over the different steps so first step generation block this is the output of the generation Block in blue then the reflection output of the reflection Block in green and we keep going so second step and we generate the python code again with all the feedback provided by the reflection block and we
[00:19:50] provided by the reflection block and we keep going and going and going and here we have the final result this is the last generation you can uh think of it as the best generation so far before we stopped the loop at the three steps uh and yeah you can see that the llm is responding to the reflection um to the reflection block by saying that it's going to incorporate some brief explanation of Mer short which I guess it's
[00:20:20] Mer short which I guess it's Incorporated here in the dock stren uh it's also adding the time complexity space complexity Etc and it's creating as you can see here this main function when it's going to put this example usage and also calling the the main function uh directly from [Music] here so today we're going to move into the second pattern that is the tool
[00:20:50] the second pattern that is the tool pattern and before we begin I'm pretty sure that you're already familiar with this pattern in a practical sense what I mean by this is that you have probably used in the past Tools in Lan chain in llama index or in crew Ai and the thing is that in today's video I'm not going to teach you how to use these tools in specific Frameworks I'm just going to teach you how these tools work under the hood and I think that's really insightful because if we really understand how things work under the
[00:21:21] understand how things work under the hood I think it's much easier for us to learn how to apply them in the proper way way so as we did in the previous video we are going to start with a Jupiter notebook that covers all the theory step by step and then I will move into vs code where I will show you all the instructions and all the classes that I have implemented to make this tool more robust to try to mimic the structure that all of these Frameworks offer at
[00:21:51] that all of these Frameworks offer at this moment you know having like a tool class and a tool agent class very similar to what we did with our reflection pattern but with with the tool pattern okay so let's begin with the theory of the tool pattern you have this diagram right here that tries to offer a simplified um description of what the pattern does or tries to implement under the hood but basically let's start by defining what is a tool and a tool let's put it uh in
[00:22:22] is a tool and a tool let's put it uh in simple terms it's just a way for the llm to access the outside world what I what do I mean by this uh remember that llms store all the information in the weights so when you ask an llm about specific information that information is going to be retrieved by the weights but sometimes the information stored in these weights is not enough and we need a way for the llm to access the outside world and that's exactly what a tool
[00:22:53] world and that's exactly what a tool does a tool is just uh like a python function that the llm can access and run and fetch some relevant results using an API or uh parsing uh web content or um Consulting uh wfr Alpha to to calculate some difficult integrals but you get the point it's a way for the llm to get outside the information stored in its weights okay so let's start by defining
[00:23:24] weights okay so let's start by defining a simple python function you have it in here so uh this this python function which uh I'm a bit ashamed of it because it's uh too simple uh basically gets the current current weather and as you can see uh if location is uh Madrid it's going to return a temperature of 25 uh it barise on the unit that you want to to put but given that it's Madrid it will be unit Celsius so it's going to return a
[00:23:55] Celsius so it's going to return a temperature of 25° C and and otherwise it's going to return 58 so as you can see don't pay too much attention to this function because it's trivial but uh it will help us to illustrate how a tool works so if we run this as I was saying is we if we run this function with location Madrid and unit Celsius is going to return this um dictionary well this string containing a dictionary with
[00:24:25] this string containing a dictionary with temperature 25 and unit uh Celsius so nothing to about this thing this is Trivial okay so let's proceed now the question is how can we make this function available to an llm because as you already know llms are just NLP systems are natural language processing systems so they expect text as input but we need a way to for the llm to really understand that this is a python function and I can call this python function to retrieve some
[00:24:56] python function to retrieve some relevant results and how can we do that okay so what I propose here is to use this system prompt so as you can see in this system prompt we are telling the LM to behave as a function called in AI model we are going to provide the function signatures within this XML TX this uh tools tax and you may call one or more functions to assist with the user query don't make assumptions about values blah blah blah okay but the important thing is that we are going to pass all the
[00:25:27] is that we are going to pass all the relevant information relevant information within this XML TX and the llm is going to return the function code inside this XML tags okay this tool underscore tag uh underscore call sorry you can see here an example of how we expect the llm to return the tool code it's going to be something like this we are going to uh the LM is going to provide a name the name of the function and also the argument that we
[00:25:58] function and also the argument that we need to use to retrieve the relevant information with this python function and then a list of the available Tools in this case uh I'm just using this one like get current weather because uh I needed to hardcode everything for this uh tiny example but as you will see in the vs code we are going to make it automatic so given a python function we are going to retrieve all of this information all of this uh function signature it's going to be retrieved automatically in the code uh
[00:26:30] automatically in the code uh implementation but yeah if you check the way the information that we are providing for each tool you can see that we are providing the name of the tool a description this is something that we can get from the do streen by the way we will see that later but yeah like get the current weather in a given location blah blah blah and then the parameters where we are putting all the different parameters and this is really important the type of these parameters in this case both the location and the unit are
[00:27:00] going to To Be Strings but suppose that we are passing I don't know um the month and we want it to behave like an integer then we should put that type inside the the function signature okay so now that we know how this system promt works let's put it into practice just a quick reminder today we are going to use a different LM than the previous video in the previous video we were using Lama 370 billion but today we are going to use uh slightly different llm because
[00:27:31] use uh slightly different llm because it's the Lama 370 billion tool use so it's a version of Lama 3 that's been uh fine-tuned for Tool use and that's exactly what we want to do today so it made sense to to use this llm okay uh we defined uh a constant uh the system prompt um where we copy and paste the system prompt that I Shar with you uh right in the in the cell below and and now let's run this cell we are going to ask the LM what's the current
[00:28:03] going to ask the LM what's the current temperature in Madrid in Celsius going to add the system prompt and we are also going to add the user uh message to the history and yeah let's run this okay so as you can see we are having a structure similar to the one we ask for the llm to return in the system prompt the LM is returning the name of the tool and it's also returning the arguments since we
[00:28:33] also returning the arguments since we ask what's the current temperature in mrid in Celsius the arguments is going to be Madrid as the location and Celsius as the as the unit okay but now this is not usable for the by the llm I mean we have a string and inside that string we have this dictionary inside these two XML tags so we need a way to get rid of the XML TXS and also transform this dictionary this
[00:29:03] and also transform this dictionary this string dictionary into a proper dictionary using the Json package the Json Library okay and that's exactly what this function does this function will get rid of the tool call or to be more specific it will gather it will get the code inside the tool call XML TX and then it will transform that string dictionary into a proper dictionary
[00:29:33] so let me show you how it works uh but as you can see when we call this part tool called string this method to the output the output remember that it's uh this one here it's going to return a proper python proper python dictionary and now if we run the get current weather the function that we Define at the beginning of the notebook if we run this function with the parameter meters that we have just pared it will
[00:30:04] pared it will return the result so temperature 25 and unit it's going to be Celsius okay without any information about the XML tax that's something that we want to get rid of nice okay so now we have the result as you can see it's this python dictionary right here but we are not over because we don't want the LM to respond with this uh structure I mean if I ask the LM for the current temperature in Madrid I expect the LM to respond me something like the current
[00:30:35] respond me something like the current temperature in Madrid it's uh is 25° C for example but not something like this not this uh dictionary so the last thing that we need to do is to add this observation the dictionary in here to the chat history okay and we are going to add this into the prompt this observation observation uh the observation text into the prompt and finally just call the the agent and as you can see the result is
[00:31:07] agent and as you can see the result is exactly what we expected so the current temperature in MADD is 25° C okay so now this is everything for this dynamic or stepbystep way of doing things but as you might imagine this is not scalable I mean we can't generate this function signature for everything that we are going to build I mean we could but it's not going to be efficient we need a way for the agent to given a python function being
[00:31:38] agent to given a python function being able to extract the function signature by signature I mean this type of structure right structure right here and also to decide between different different tools so instead of doing all of this process we need the agent to extract all of this logic away from the user and to do it under the hood and that's exactly what we are going to do right now the logic that I'm going to show you in vs code how to implement all of
[00:32:09] code how to implement all of this the proper way so let's get into vs code okay so here we are in vs code let me show you the new modules that I have added to the repository so if you go to the source identic patterns folder you will find a new folder the tool pattern folder and inside you have three modules the tool agent the tool and the utils uh let's begin with the tool cuz I think it's uh the most important topic uh of
[00:32:41] it's uh the most important topic uh of today's video and the tool agent at the end of today is just a way to interact with a tool okay so this module starts by implementing a method that allows you to get the signature out of a python function so this is basically the mo the the method I'm referring to to it receives as parameter uh function and it will uh get the schema and out of the schema also the function signature and the function the function signature it's basically the structure
[00:33:12] signature it's basically the structure that we defined on the system prompt previously all previously all right next we have this class right here tool class that has three attributes a name the function and the function signature as you you might imagine uh it's going to be generated by this function right here and the function is basically the function that we want to call when the llm uh decides that it wants to use a specific tool this function is the
[00:33:43] specific tool this function is the python function that's going to be used under the hood and then we have this tool decorator that uh can be used to decorate your python function and to automatically transform the python function into a tool object uh if you inspect a little bit the implementation of these aurator first uh you can see that it generates the function signature out of the get function signature method that we explained uh before and then in returns a tool object by uh defining the
[00:34:13] returns a tool object by uh defining the name using the function signature passing the the function that you are decorating as the function attribute that the tool expects and finally getting the function signature uh from the variable that we defined previously because remember that we were getting the function signature using this method and and yeah and having these three attributes we are able to to generate a tool okay now let's move into the tool agent which as you can imagine
[00:34:43] the tool agent which as you can imagine is an agent that has the capability of using tools you pass a list of tools and it will uh select the proper tool the the right tool for the specific question that we asking and then it will run the tool to fetch the relevant details that it needs from the outside world and then returning all this information in uh natural language to you okay so things that you are already familiar with so this tool system prompt is basically the
[00:35:14] this tool system prompt is basically the one that we explained earlier in on the video uh and then the tool agent consists of following attributes so it we need to generate uh the the grow client then the model that remember that by default we are going to use the Lama 370 billion tool use and then this is the the important part this is the the tricky part of this agent but we need to define the list of tools that we are going to
[00:35:45] the list of tools that we are going to to use for this agent and then this list of tools are going to be used in the ram method so the ram method uh consists of the following steps first of all we expect this user message and we transform the user message into a user prompt using the open AI API definition then we are going to generate both the tool chat history and the agent chat history and now we are going to generate the first completion we are
[00:36:16] generate the first completion we are going to make the first SC to the gro model and what this is going to do these two blocks of code is to generate basically the logic that we explained in the notebook let me be specific so it's going to first of all return the tool call call okay this first uh call uh this tool call string is basically this output and then the par
[00:36:47] basically this output and then the par tool called string it's a method that mimics the same logic that we implemented in this function okay so at the end this uh tool call is going to be something like something like this okay so now that we have the tool call information we can get the tool name from from this object from the tool call we can also get the the tool by using this tool stick because now that we have the tool name
[00:37:17] because now that we have the tool name we have also defined a dictionary that contains a relationship between uh the tool name and the tool okay then we are going to validate the arguments so to to make sure that if uh the function expects uh string the llm is not sending an integer we want to make sure that the types that the llm has generated in the tool call and the types expected by the python function match okay and then we are just going to run
[00:37:48] okay and then we are just going to run the tool with this tool run and we are passing the arguments that we have just uh defined on the tool call remember that if we go to to a tool call remember that we had this arguments key that contains the arguments and its values to to achieve the to retrieve the the proper proper information okay and finally we are going to append this result to the chat history and remember that we are adding this by using this observation prompt
[00:38:20] this by using this observation prompt okay so now the only thing that's missing is to make another call to to the llm in Gro and we will receive the outut okay so now that we understand how all of these classes and instructions work I think it's going to be really cool to see everything in action and that's what we are going to cover next so uh everything is inside this section of implementing everything the good way of course you have to understand that this implementation it's not like the
[00:38:51] this implementation it's not like the perfect implementation because uh I'm not trying to create another framework and I'm just trying to make something that's uh well implemented but at the same time easy to understand so so yeah uh just bear in mind that we are not trying to to create another agentic framework in this case Okay so uh let's continue uh let's see how the tool decorator works and instead of using some dummy uh function
[00:39:21] instead of using some dummy uh function in this case we are going to implement something more uh something closer to to reality something closer to the tools that you might be wanting to implement in the future so uh in this case the the function that I have implemented it's a function that fetches the top end stories from Hacker News if you don't know what Hacker News uh is it's a very famous uh page where you have different types of of stories and many
[00:39:52] different types of of stories and many of them uh link to some article another to GitHub repositories to tweet to whatever and it's very very used by by a lot of people so I thought it would be cool to have this uh this function that allows you to retrieve a top number of these functions of these uh stories sorry and and yeah to convert this to transform this function into into a tool okay so let me show you first of all that the python function works
[00:40:23] all that the python function works properly so if we run the fet stop Huger news stories with a top end of five it's going to take the the top five stories uh let's check the first one to Too Much efficieny makes everything worse and if we go to Hacker News web page you will see that yeah that this is the first story so everything seems to be working fine now let's transform this python function into a python tool and we are going to do it by using
[00:40:55] tool and we are going to do it by using this method that we covered uh previously okay so now that we have uh run the tool method the hn tool is going to be a tool we can access the name of the tool and we can access the function signature that as you can see contains all the information that we put in the system prompt at the beginning of the video but right now the cool thing is that everything everything has been generated automatically and yeah you can see here
[00:41:26] automatically and yeah you can see here that uh uh has a description and the description has been retrieved from the dock stream and we have also the parameters here uh in this case it's a very simple function so we just uh need this top end argument and it's of type integer so everything seems to be working fine and now let's move into the tool tool agent so the tool agent to instantiate this tool we just need uh a list of tools in this case we are only using in
[00:41:57] tools in this case we are only using in one Tool The hent Tool and now let's uh run the agent and in this case uh I wanted to check that everything works properly by doing the following strategy so first of all I'm going to ask the agent about something that it's not related to Hacker News so for example tell me your name and if everything works properly we should see yeah something not related with the agent with the tool sorry and as you can see giving the output the agent has not used
[00:42:28] giving the output the agent has not used any kind of tool and that's the proper way to work because uh if the user message is not related to any tool we don't want the agent to spend time on interacting with tools but what happens if we ask the same agent about the top five Hacker News stories right now so in this case we should expect the agent to use the tool as you can see uh I have added some login to make it uh easier to see but check this
[00:42:59] make it uh easier to see but check this so the agent is using uh the tool the fet stop Hacker News stories it's using the tool with this C dict so this is the name and the arguments the top end with a value of five and finally is generating a result but remember that we don't want this kind of result I mean if I'm asking about the five top stories in Hacker News right now I'm expecting something easier to understand and that's what we
[00:43:29] easier to understand and that's what we achieve if we print the output and here we have the five top stories in Hacker News the first one is that the article about too much efficiency makes everything worse that we saw in The Hacker News page and if we click the URL attach you can see that everything seems to be working fine I mean it's not like the agent redirected as to some broke and URLs I mean the URLs are real and
[00:44:00] and URLs I mean the URLs are real and it's uh it's working as expected so yeah this is everything I wanted to teach you about tools My Hope Is that now when you start using or keep using uh tools from BL chain Lama index or crew AI you have a deeper understanding how these objects uh work under the hood [Music]
[00:44:30] today we are going to focus on the planning pattern and we are going to implement a react agent using the react technique maybe you are familiar with the term react but uh more related with front end development because of the front end framework today we are going to talk about a different react this react stands for reason and act and it's based on a paper that I have shared with you on on the Jupiter Notebook on the on the introduction the introduction is based on a paper that tries to
[00:45:00] is based on a paper that tries to improve the planning or the reasoning capabilities of llms uh as I was saying I will leave you the link here but um I don't want to get into the paper here so what I will do is to show you a simplified version of this react agent by using this beautiful diagram so this diagram showcases really well how the react technique Works under the hood it uh it's basically a loop that consists of three different components or three
[00:45:31] of three different components or three different steps the action step the observation step and the third step the action step is basically running a tool it's nothing more than that the agent decides to use a tool and it runs the tool then the observation step is the step in which the LM is going to make an observation about the output of the tool then we have the third step where the agent is going to reflect on the
[00:46:01] agent is going to reflect on the observation that is just made and it's going to decide what to do next maybe it decides to call off the the iteration process so the agent finishes the the loop and it returns a response to the user this output in here or maybe it decides to use another action and continue the iteration all right so that's basically the the technique or The Loop the logic that we are going to to implement today so now let's go into the as always into the Jupiter
[00:46:32] the as always into the Jupiter notebook here you have as as always a short introduction about the planning pattern what the react technique is um also the link that I told you about uh before for the for the react react paper and links to the previous lessons uh because many of the things that we will do today are based on the previous lessons so if you haven't watched the videos and you haven't checked the code I recommend you to to do it because uh
[00:47:03] I recommend you to to do it because uh many of the things even some utils that I'm going to import or how we are going to create the tools is based on the previous video so I recommend you to to take a look all right so let's get into relevant Imports and the gro client as you know this series is based on Gro models we are not going to use any kind of framework no Lama index no lanch no crei just uh Gro calls gr API calls so in here we are just going to to import uh some typical python libraries
[00:47:34] import uh some typical python libraries and also the grow client and we have here some utils one of the utils is this tool decorator that we use in the previous video remember that this tool decorator uh is able to transform automatically a python function into a tool right to a tool object and this extract content which is something new I created for for this video is a very similar function that the only thing that it does is to given a test input
[00:48:04] that it does is to given a test input it's going to retrieve the content within some XML TXS maybe it's very abstract right now but when you see in action it's going to to be real clear oh and by the way I'm using this Lama 3.1 70 billion parameters uh in the previous video we were using Lama 370 billion fine tune for Tool use today I wanted to try another Grog model so I decided to use this one and the results are really really cool all right so let's run this
[00:48:34] really cool all right so let's run this cell and let's go into some of the tricky parts of of the react implementation which is the system prompt okay this is uh the system prompt if you take a look uh a quick Lance at first it will be really will appear to you really similar to the tool system prompt that we implemented in the previous video but it has some differences for example I'm telling the D llm to behave like or to operate by running a loop with the following steps thought action and observation which as
[00:49:05] thought action and observation which as you can imagine is linked directly with this diagram I'm telling the llm to behave like this to implement or to select the operations from these three operations thought action and observation which are the building blocks of the react technique as we explained previously all right so apart from that you can see that everything is the same we are telling the LM to return the tool calls enclosed by DS XML TX also we are defining the available tools
[00:49:35] also we are defining the available tools this is something that we are going to part that we are going to complete when we have all the tools defined and the function signature function signature generated and now the example session this is the most important part because it's the moment when we defined the react tool the react Loop sorry one thing that I want you to notice is that I'm using XML tax for every message and why am I doing this so I discovered that D LMS are working much
[00:50:06] I discovered that D LMS are working much better when I input this kind of structure messages and that's the reason why I chose to make uh everything this way I mean I I could do something like question semicolon and uh thought semicolon uh things like that but I discovered that it was uh not as accurate I mean it was difficult to pass the the output when when I was using this kind of a structor and it worked uh much better like this and I think it's
[00:50:36] much better like this and I think it's even much clearer uh if you want to read it I mean at least for me it's much more elegant this solution okay so let's get into the loop so first of all we are going to ask a question and this question is going to be enclosed by the question uh XML tax then the LM is going to think about this question this is like an llm internal thought enclosed by the thought XML tax I need to get the current weather in Madrid then the llm is going to provide a tool
[00:51:08] then the llm is going to provide a tool call it's going to select a tool from the available tools and it's going to provide uh a way to to run this tool all the arguments necessary to run this tool in this case it's going to use the get current weather and the arguments are going to be uh location Madrid and unit Celsius Celsius all right then the llm is going to be called again with this message an observation message remember that this is run the observation was the step just after the action
[00:51:39] after the action step and this observation which is as well enclosed by the observation XML tax contains the output of of the tool in this case temperature 25 and unit Celsius then it outputs a response enclosed Again by its specific XML TX the current temperature IM Metate is 25° C okay so if the agent returns a message like this this response message it means that the loop has to be uh finished because uh it has a final response of
[00:52:09] because uh it has a final response of course um in in practice this is not going to be the case because maybe after this this observation the llm decides after the thought uh process to use another action and then think again about the output of the action and use another action I mean the the loop could be be extended by any iterations as needed but the moment that yellow L returns this response message then it indicates that the the agent is going to stop the loop all right and finally I've added some additional
[00:52:40] finally I've added some additional constraints uh if the user ask you something unrelated to any of the tools above answer freely enclosing your answer with response TX so this is basically because uh suppose that I ask the llm uh what's his name or uh to write me a poem in this case I don't want the LM to run a tool for that because that that's not needed I mean it C default to its own weights to generate a response it's not necessary for the LM to access
[00:53:10] it's not necessary for the LM to access the the outside world with any tool so that basically that's basically what this constraint is doing all right so let's run thisel now we have the the react system prompt in memory and let's start the example step by step so we are going to start by defining the tools uh in this case I chose to create three tools as you will see uh they are very basic tools uh we have the sum two elements tool which accepts two parameters A and B two integers and as
[00:53:42] parameters A and B two integers and as you can imagine it sums the two the two elements then we have the multiply two elements which as you can imagine as well uh it multiplies A and B so nothing fancy here and finally uh the compute BL which surprise surprise it also uh it's basically uh the logorithm of the the input this x which is going to be an integer all right and remember uh one thing that we are going to define the tools by using the tool decorator which
[00:54:13] tools by using the tool decorator which uh we defined on the previous video and what it does is to transform the python function into a tool object all right finally we are going to Define this uh dictionary that relates the two name with the tool object and we are going to yeah and we are going to verify that everything works by um using the Su two elements and accessing two of the primary attributes of the tool so if we run this cell you will see that we have the tool
[00:54:44] cell you will see that we have the tool name Sam two elements and also the tool signature which contains the name it also contains the description with the dock string and this is the most important uh argument the parameter which contains the properties a of type int and B of type int as well so everything seems to be working fine now let's get into the into the system prompt again because we need to add the tools uh the tool signature the signature for the three tools that we
[00:55:14] signature for the three tools that we have just implemented into the react system system prompt all right so uh first of all I'm going to create this variable by concatenating In This Scrappy expression the three function signatures let's check it but as you can see we have the signature for the sun two elements tool the signature for the multiply two two elements tool and the and the signature for the compute log tool all right and now let's uh simply add this tool
[00:55:44] now let's uh simply add this tool signature into the system prompt and let's PR the let's print the system prompt to see that everything is worked fine and as you can see in the tools XML TX we can see enclosed the the three function signatur so everything seems to be working smoothly until this point now we have everything ready to start implementing the react Loop and I have divided this Loop in different steps uh to make it easier for understanding Let's uh so let's begin with the the first step involves the
[00:56:16] first step the first step involves the user question in this case I wanted to ask the react agent this question I want to calculate the Su of 1 2 3 4 and 5 6 7 8 and multiply the result by five then I want to take the logarithm of this result okay this is my question so the first thing that we are going to do is to generate or create this chat history using the system prompt that's the first uh message the first um prompt that we are going to add into the chat
[00:56:47] that we are going to add into the chat history and then the user prompt and remember that we are going to use all the time this tax this XML tax in this case since uh it's the user question we are going to use the question tag all right so let's create the chat history and let's run the first completion all right so take a look here we have two messages the first one is a thought message I need to calculate the sum of two numbers and multiply the result by a specific number and finally calculate the logarithm of the result and then we have a tool call uh which is going to
[00:57:19] have a tool call uh which is going to call for the Su 2 elements tool which seems uh okay and then it's going to use this argument so a a is going to have a value of 1 2 3 4 and V of 5 6 7 8 so everything seems to be working fine the llm uh has chosen to use the Su two elements first and it makes sense because um if you look at my user question I'm asking uh first of all to sum 1 2 3 4 and 5 6 7 8 so it makes sense to uh for the llm to have chosen
[00:57:51] sense to uh for the llm to have chosen this this action as the first one now let's add this output to the chat history as a system roll type and now let's uh get into the step two so in this second step we are going to use the extract tag content that I mentioned previously this extract tag content is going to take all the text enclos by the tool called XML tags in this case it's going to be basically this all right
[00:58:22] going to be basically this all right it's going to take this dictionary this string dictionary all right going to yeah and as you can see this Tool uh this tool sorry this stct tag content sorry um is not going to return the plain string is going to return uh data class so we need to access the content attribute all right and then inside the content attribute we need to access the first index because it's um it's going to provide addiction um a list and we need to access the the first element in this case all right so as we
[00:58:54] element in this case all right so as we can see the dictionary seems pretty good seems fine and now we just need to run the the tool so we are going to access the tool from the the dictionary of available tools and we are going to to run this tool with the with the AR with these arguments all right and now we are going to make this assertion because I wanted to make sure that the tool result it's the same and it needs to be exactly the same as the mathematical result that we are sure of uh so yeah this assion uh
[00:59:25] we are sure of uh so yeah this assion uh basically basically computes that and since we are not getting any assertion error it means that everything is working fine and now let's uh add this um this content into a chat history in this case the RO type is user and remember that we need to use the observation XML tag to add this observation into the into the agent because remember that just after we execute an action the react technique is going to perform an observation on the tool output and and that's exactly what
[00:59:56] tool output and and that's exactly what we are doing with this XML tag here so let's add the chat history and let's move into a step three so now let's run this completion and let's see what the LM has to say and it's going to think again about the about the observation that it it's just it just made so the thought uh says the following I have the result of the sum now I need to multiply it by five all right so it's sensible it
[01:00:27] it by five all right so it's sensible it makes absolute sense and the tool call that is going to use is the multiply two elements so perfect and the arguments are going to be 6 91 2 as a and five as B so it makes sense makes absolute sense Let's uh let's set the output into the chat history with a rooll type of assistant and let's move into a step four I'm not going to get into the details of the these uh cells because
[01:00:57] details of the these uh cells because it's basically the same as we did here okay but for another tool so just run this uh check the tool result this is basically the multiplication of 6912 and 6912 and five and now uh let's um corroborate that everything is working fine which it does and let's add the result into the chat history chat history and remember we need to add this result
[01:01:28] and remember we need to add this result as an observation XML tag all right all right so let's get into step five let's run the completion and let's see what the llm has to say again and in this case uh it says that it has the result of the multiplication but now he needs to calculate its logarithm so perfect sense again the tool call in this case is going to be compute log and the arguments the arguments 3456 uh 34560 is the result of the sum plus the
[01:02:00] is the result of the sum plus the multiplication all right so let's append this assistant message into the chat history and let's get into step six where we are going to run the last tool of this of this Loop this is the logarithm after we compute the logarithm of 3 4 5 6 Z and let's assert let's make the typical ass assertion to check that the mathematical um computation is the same as the tool result now let's add uh the user message
[01:02:32] result now let's add uh the user message into the chat history remember once again uh I want to remind you of this uh over and over we need to put the observation XML tax for everything to work uh smoothly and properly and finally we are getting into the step seven which is going to be the final one and if we run this you will see a beautiful XML uh tag saying response which means that the agent has decided that the loop is over and now uh we can read the message the result of
[01:03:03] we can read the message the result of the calculation is a logarithm of approximately 10. 45 which is exactly the result that we got here after applying the to result so we have managed to make this react logic work but as you can imagine this is not the way uh to implement it uh like the good way because we are not going to implement each one of the iterations from scratch and manually we need a way to implement everything automatically and that's exactly what I'm going to
[01:03:33] and that's exactly what I'm going to show you right now in vs code and then we are going to see all of that code in action in the next section but first let me show you how the react agent is implemented in the code base so let's get into vs code okay so here we are in vs code so as we in previous videos you just need to go into the into the source folder agent patterns folder folder and inside just click the folder related with the topic of of the video so today we are uh learning about the planning
[01:04:04] we are uh learning about the planning pattern so simply uh get into the planning pattern folder and inside click the react agent and if you scroll down in this module you will see uh some familiar things like for example this react system prompt this react system promt is basically the same that we used in the Jupiter notebook it uh I mean it's B it's exactly the same I just copy and pasted this react system prompt to The jupit Notebook so there there are no Mysteries here if you keep scrolling
[01:04:34] Mysteries here if you keep scrolling down you will see the react agent the proper implementation and this is uh where you will see some uh some differences with the code that we implemented in the notebook so first of all uh as attributes we are defining the RW client also the model that by default remember that we were using Lama 3.1 70 billion billion uh a list of tools and the tool stick that we defined also in the Jupiter notebook but in here it's going to to be
[01:05:04] notebook but in here it's going to to be an attribute a class attribute all right then we are defining as method this add tools signature uh that remember that we did something very similar in in the Jupiter notebook but in this case uh it's not as Scrappy as the implementation that we did in the notebook uh this is much more subtle much more uh efficient and then we go into the process tool calls uh this is some of the core logic of both this agent and the tool agent
[01:05:34] of both this agent and the tool agent and let me read to you the docker uh the dock string because it provides a really good explanation of what this method does so it processes each tool call it validates arguments it executes the tools and collects the results and as you can see it's not that different from what we did in The Notebook but this is like a proper implementation and one of the key differences is the is this uh validates argument uh functionality
[01:06:04] validates argument uh functionality because in this case we are not just uh running the tool and getting the results this case we are also validating the the input to that U to that tool because we don't want to end in a scenario where a tool is expecting an integer and we are inputting a string for example that's something that might happen and we want to contemplate these type of situations all right so but that's basically what this uh method is going to do it's just processing its Tool uh running its tool and and collecting all the results and
[01:06:35] and and collecting all the results and returning the the results with um this observations variable which you as you might imagine is very it's linked to the diagram that that I showed you at the at the beginning of the video all right and now we have the last method the run method this is a common method on the three agents that we have implemented so far far the reflection agent the tool agent and now the planning agent the react agent are implementing this run method and the difference between this
[01:07:06] method and the difference between this react agent and the previous uh tool agent is that in this case we are running another loop inside the Run function and this loop as you might imagine is the react Loop okay so let's go into the different steps of this Loop but first of all we are going going to run the completion then we are going to check if this completion contains a response XML tag in that case we are going to cancel the loop we are going to
[01:07:36] going to cancel the loop we are going to Simply return the response because remember that the response XML attack means that the agent has decided that that's the end of the loop it has uh discovered a response it it has a a valuable response for us so it the loop can be can be cancelled okay if we continue you will see this thought and this tool colors variable the thought variable is uh basically the thought content all the content enclosed
[01:08:06] thought content all the content enclosed by the thought TX and the tool call is basically the same but for the Tool uh for the tool call that's exactly what we did on on the jupyter notebook all right so after this we update the chat history with the completion using the assistant role and then we print some beautiful Logs with um with colorama using magenta in this case and we are plotting the we are printing the thought you will see this in in action in a a few minutes later when we run this in the jupyter notebook but but that's basically it I
[01:08:36] notebook but but that's basically it I mean we are just um printing the thought and then we are going to check if there are some tool calls in the completion if that's the case we are going to run the process tool calls that is the method that I explained to you before where we are going to run all the different tool calls that we need and we store the result in this observations variable and we print the results with another color in this case blue and these are basically the observations uh if you remember the
[01:09:06] observations uh if you remember the diagram the react diagram all right and finally we are going to update the chat history using uh user R type and we are updating with the observations that's the message that we are going to add into the chat history list okay and and that's it uh then we start again the loop and we keep iterating so there are two things uh that might happen that uh return a value to the to the user the first one is that the agent finds a response XML tag as we
[01:09:37] the agent finds a response XML tag as we already mentioned the other one is that uh we get into the max rounds into these Max rounds that I defined at the beginning of the of the run method that defines the maximum rounds for the react logic to to iterate and if we go beyond this Max rounds it means that the agent hasn't found any response tag on the completions so what we are going to do is simply to return the completion of the of the current chat history all right so that's basically
[01:10:08] history all right so that's basically the logic as as you can see it's really simple I mean it's just a simple loop with I don't know 20 lines of code and but yeah but this is basically uh a version of the of the react technique okay so now let me show you this inaction in the jupy notebook with all the colors and colorama Etc I think you're going to to like it okay so the first thing that we are going to do is to import the react agent and then we are going to instantiate this react agent using the three tools all
[01:10:38] agent using the three tools all right and now let's run we I I just copy and pasted the user message that I that I use here that I created here so let's run this and see what happens okay everything seems to going really smoothly all right this is really cool so uh as you can see the the colors improve a lot the readability so we have a first thought I need to First calculate the sum of 1 2 3 4 and 5 6 7 8
[01:11:10] calculate the sum of 1 2 3 4 and 5 6 7 8 they multiply the result by five and finally compute the logarithm of this result then we are going to use this results this is basically the action uh some two elements this is the tool called dict some two elements and 1 2 3 4 5 6 7 8 and the tool result is this one and the observations which is uh 6912 then there's an additional thought now I need to multiply the result by five and we are using the tool multiply two elements tool called dig multiply two elements arguments 6912 the result
[01:11:41] two elements arguments 6912 the result of the previous iteration and five which is the the multiplier and the tool result is result is 34560 all right so the observation is basically that result and a last thought now I need to compute the logarithm of this result so the llm chooses to use in this case the compute log action compute log tool and it computes the log by using using 34560 that's it the tool result of the
[01:12:11] 34560 that's it the tool result of the previous previous iteration and finally we get the this result uh 10. 45 and the observations are basically that result so the final answer is that the sum of 1 2 3 4 and 5678 is 5678 is 6912 multiplying this result by five giv gives gives 34560 and the logarithm of this result is is 10.2 10.2 45 and as we can see if we scroll up that's exactly what we achieved the
[01:12:43] that's exactly what we achieved the result that we got when we were running everything step by step I hope that you are excited because you have successfully implemented the react technique which is uh one of the most known techniques for reasoning for improving the reasoning capabilities of llms in fact if you go to Lama index if you go to crei or to L chain you will see that the core agents are implementing this technique so congratulations because you have learned how to implement a very very important
[01:13:19] technique and today we are going to to learn about the last pattern the fourth pattern the multi-agent pattern in case in case you are not familiar with this pattern and in case you haven't used tools or Frameworks like autogen crew AI or even uh L graph I will give you like a very quick summary about this pattern but basically what this pattern tries to achieve is to divide a task into smaller into simpler subtasks that are executed by different agents so uh each agent is going to act
[01:13:52] agents so uh each agent is going to act is going to adopt a role for example One agent could be a software engineer another agent could be a project manager and another agent could be the CTO of a company and each one of these agents are going to solve smaller tasks okay so when all the smaller tasks are solved then we can estimate or we can say that the the bigger task is going to be solved by the crew of Agents by the multi-agent application okay so uh this
[01:14:22] multi-agent application okay so uh this is basically approach uh remember that in the react application in the react Technique we have an agent that has at its disposal a lot of different tools and it can choose between the different Tools in this case we are going to make the agents uh like smaller like dumber and and each agent is going to be uh assigned to a specific task okay that's basically the basically the approach uh I wanted to mention crew AI as I was saying because uh maybe you are
[01:14:53] as I was saying because uh maybe you are not familiar with this framework but uh I've done a couple of videos about this framework previously in the channel so take a look if you want and now we are going to get into the Jupiter lab Jupiter notebook as always and and yeah I'm going to get or to explain in a bit more detail what I was uh saying before so what's my Approach today what am I trying to achieve with these videos uh so when I started implementing the
[01:15:24] so when I started implementing the multi-agent multi-agent pattern I was going to follow the same approach in previous videos like doing a very uh simple a very Scrappy implementation in the Jupiter notebook and then teaching you how to implement it the good way or the proper way in vs code but today I'm going to do something a bit more different and I wanted to make something more elaborate uh elaborate from from the beginning okay that's what I've been working on like a minimalist version of crei that uh draws
[01:15:55] minimalist version of crei that uh draws inspiration from some of the key abstractions in CI particularly the crew and the agent okay we will see them in in detail later but just to let you know and apart from that I also wanted to um draw some inspiration to to borrow some ideas from airflows design philosophy using the right shift and the left shift operators by the way if you don't know what airflow is is um it's the orchestrator one of the most famous orchestrators and it lets you define the
[01:16:25] orchestrators and it lets you define the dependency between the tasks using the right shift and the left shift operators this uh this operator right here okay and in my implementation I'm going to borrow this idea I'm going to overload this operator and instead of using tasks instead of defining the dependency between different tasks I'm going to define the dependency between agents and instead of uh defining a dck uh like in airflow this um dck over here we are
[01:16:55] airflow this um dck over here we are going to define a crew but just the same way like defining one session in Python and defining the different agents and the dependencies between the agents um inside that session inside that crew session okay uh right now it might be a little abstract but you will see that it's um it's quite simple When You See It In Action okay so let's get into the Jupiter notebook uh here you have as always the the introduction basically what I've been saying right now you have
[01:17:25] what I've been saying right now you have also links to the previous lessons to the first lesson about the reflection pattern the second lesson about the tool pattern and the third lesson about the planning pattern uh and now we are going to get into the first class of this mini crew aai uh framework which is the agent class okay so as you can imagine we are uh building a multi-agent framework so it makes sense to create a class for the agent for the proper agent and as you can see uh we just need to to import the
[01:17:56] can see uh we just need to to import the agent class from multi-agent pattern agent and we can start using the agent but let me show you the implementation first uh because I want you to understand what we are doing under the hood so we are going to to go into into vs code and I will show you how it works under the hood okay okay so in vs code you will see a new folder inside the agentic patterns folder called multi-agent pattern and inside you will see two modules the agent and crew okay we will start with
[01:18:28] agent and crew okay we will start with the agent and then we will see the crew implementation so first thing I would like you to notice is related with the Imports because even if we are importing This Crew object that you have not seen yet uh there are two abstractions that are important here that you have already seen in this series the first of all is the tool let me get into the tool uh that we implemented in the second
[01:18:58] uh that we implemented in the second lesson of this series and the second thing that you should be already aware is the react agent that we implemented in the previous video okay in the third lesson of the series remember that we were defining all this react system prompt in the previous video and we saw how uh the react technique could be implemented in very easiest steps okay so so yeah this agent is going to be based both on the tool because an agent can use multiple tools and on the react agent because
[01:19:28] tools and on the react agent because under the hood this agent class is going to be a react agent class okay okay so let's get into the init method and as arguments we are going to pass the agent name uh the backstory and if you have worked with with crew AI previously you should be really familiar with this uh naming because it's basically the same uh the backstory is uh like a background for the agent and in some tasks it might help um it's very similar to the system
[01:19:59] help um it's very similar to the system prompt when you are dealing with a with a bare llm um then we have the task description which is uh the description of the task assigned to the agent the task suped output this is uh becomes really handy when you want to define a very specific output for the task which is uh the spected format or content of the task output then we have the tools is a list of tools and remember that the these are not um crei tools or L chain tools or Lama index tools these are the
[01:20:32] tools or Lama index tools these are the tools that we defined on the second lesson of the series okay and finally we have the llm in this case I chose um this groc model Lama 3.1 70 billion versatile and it works uh very well so that's the reason why I chose it okay so as attributes uh well we Define the name back story T description t expected output and and the react agent uh importing the class that we
[01:21:03] agent uh importing the class that we talked about just before now if you keep scrolling down you will see two attributes uh the dependencies and the dependence the dependencies I mean if you think of a multi-agent application multiple agents related with each other the dependencies are basically the agents that this agent depends on okay okay so if these agents that this agent depends on have not run successfully then the current agent is not going to run its task okay and the dependents are
[01:21:36] run its task okay and the dependents are basically the inverse so they are the agents that depend on this agent so if this agent this current agent does not succeed then the dependants are not going to be triggered and not going to be run then you have the context which is basically the AL put from a previous agent okay so uh imagine that this agent this current agent implemented by this class depends on another agent the context is going to be the output from
[01:22:06] context is going to be the output from the previous agent okay and then you have this weird stuff about uh register agent um this will make sense when we explain the crew but basically the crew in this uh micro framework is going to be Trad as a session Okay so so you define a session and inside this session you're going to define the different agents belonging to the crew so uh what is happening here is that if we have a crew if there there is a crew um active then this method is
[01:22:39] a crew um active then this method is going to register the agent to the active crew okay but that's something that you don't need to bother um and you will see it in action and in the Jupiter notebook okay so so don't bother about this because it's a bit weird but it made sense to to use this this session okay this um this screw context as you will see later okay we keep scrolling down and you will see a lot of uh shift methods the right shift the left shift and and related methods that basically
[01:23:10] and and related methods that basically um allow you to use let me go to the this is something that we haven't seen yet but basically allow you to to use these operators okay the ones that we were talking at the beginning that were Borrowed by airflow philosophy and that um Define the dependency between the different agents these are the the right shift and also the the Le shift okay the Le shift operator but um I mean in my case I've never used the the Le shift I think so I
[01:23:41] never used the the Le shift I think so I will stick with the right shift operator Okay cool so so yeah as I was saying all of these the right shift L shift Etc are related with these right shift and left shift operators and then we are are going to also the add dependency and add dependent which are basically ways to to Define this dependency between the different agents not something that I think we should invest a lot of time in you can get into the code and see uh how it it is implemented because it's you
[01:24:11] it it is implemented because it's you will see that it's um really simple and now we get into the receive context and this method is used for receiving contexts from another agent so suppose suppose the one of the dependencies of this current agent has been successfully run and we have an output from the previous agent okay so this receive context method is going to take that output and allocate that output that string into
[01:24:41] allocate that output that string into the self context attribute that we defined at the beginning of the class okay okay here of course if the if the agent has no dependencies then uh this context will be an empty string okay then we move into the create prompt because even if we are dealing with uh react agents we need a prompt to pass to the agent and in this case this prompt is going to be composed of uh several information but the most important one is the test description that's something
[01:25:12] is the test description that's something that we pass um to the init method if you recall the test expected output that's something that we also pass to the to the init method and finally the context and this context it's basically what we spoke about here in the receive context and the context attribute attribute okay yeah that's basically a prompt and then we go into the main method which is the the run method uh that simply creates The Prompt The Prompt that we
[01:25:42] have seen here with the create prom method and then it's going to run the react react agent with this prompt with the prompt that we have generated with the previous method method finally because this is not over we need to pass this output to the dependence that is remember to the agents that depend on this current agent and that's the reason where we are going to Loop through the dependence list and we are going to call the receive context method
[01:26:14] going to call the receive context method for all the dependent agents okay so that the output of this current agent is going to be uh allocated as the context for the dependents okay I know it's a bit tricky but when you think about it uh it's quite uh quite simple and and we will see it in in action with some examples and you will see that um even if the implementation might seem a bit uh difficult uh it's really simple and the and the application and the interface is really clean and and easy
[01:26:45] interface is really clean and and easy to understand so so yeah just wait for a couple of minutes okay so so yeah we Now understand how this agent be caves and now we are going to to see some examples okay so remember that we were going to to import the agent class we have already imported that and now we are going to create our first agent with this new framework um the name of the agent is the poet agent the backstory is that you are a well-known poet who
[01:27:15] that you are a well-known poet who enjoys creating high quality poetry the T description is write a poem about the meaning of life so I was feeling uh philosophical when I created this demo and the task expected output is just output the poem without any title or int introductory sentences okay so let's create this agent and let's run the the agent this remember is a react agent but it's uh really simple because uh it has no tools so it just uh it's like an
[01:27:47] no tools so it just uh it's like an input output um prompt basically I mean you give a prompt and the agent responses with with the result but without any tool involved okay so in Realms of existence we wonderand and roam blah blah blah searching for answers to an interal home so yeah very beautiful rhyme and and let's get into into something else so we can see that this works for a very simple example but now let's try uh something else with with tools let's
[01:28:18] something else with with tools let's create an agent that has tools assigned to it okay and for this we are going to import The Decorator that we defined and we implemented on the second lesson of this series The Tool decorator remember that uh this works uh almost exactly as um the tool decorator from crei from lante and from Lama index ET it's basically the same okay so the tool I'm going to implement it's called right string to txt and as you can see it's a
[01:28:48] string to txt and as you can see it's a very very complex function because it takes um a string and a file name txt file name and it writes the string into the into a file name so yeah incredibly complex and and we are going to define the agent okay so in this case the name is going to be writer agent the back story I didn't know what to put here to be honest so you are a language model specialized in writing text into txt files so forgive me for this uh I mean
[01:29:19] files so forgive me for this uh I mean uh it's terrible uh the test description is right the string this is a to tool agent into this file this tool agent example example txt and the test expected output is a txt file containing the given string and we pass the tool WR string to text okay so let's define the agent tool example and let's run this tool okay so remember if you remember the the previous video that we had this fancy login with
[01:29:51] video that we had this fancy login with colorama uh we have first of all a thought an agent thought I need to take the given string and write it to a txt file with a specified name using this tool so it makes sense right string to txt and the tool called dict is um is going to use this tool right string to txt and the arguments string data is this is a tool agent so yeah and the text file name is tool agent sample txt so it makes sense and if we go to yeah
[01:30:22] so it makes sense and if we go to yeah here we will see this um incredibly complex txt file with uh the stram this is a tool agent so as you can see our agent has successfully generated the file file okay so okay so uh this is basically the same that we saw on the previous video so now let's get into the dependencies which is uh one of the of the key Concepts one of the of the key parts of this multi-agent pattern so we are going to Define two agents in this case a poet agent agent
[01:30:55] agents in this case a poet agent agent same as before and a poem translator agent this is a poem an expert translator especially skilled in ancient Greek the task description is that this agent has to translate a poem into ancient Greek and the task expected output is um just output the translated poem and nothing else okay so let's define the two agent and this is the cool part let's define the dependency so in this case agent one is a dependency for agent two or agent two is dependent
[01:31:28] for agent two or agent two is dependent of agent one okay so let's define the dependency and now if we plot the attributes that we saw in vs code uh dependencies dependence dependency dependenc we'll see that agent one has no dependencies it's an empty list but it has one dependent the poem translator agent an agent two has one dependency the point agent but no dependenc oops sorry about that no dependence
[01:32:00] that no dependence okay now the cool thing is that if we run agent one now we should expect that agent one output is going to be pared as the context for agent two okay as we saw on the code so if we run agent one in whisper winds uh that gently sway a mystery and Falls each passing day blah blah blah if now we print the context of agent 2 you will see that the point translator
[01:32:30] you will see that the point translator agent receive context is in whisper winds the Gent is wayway a my FS is passing day so it's working it's working the context for agent two is the output for agent from agent one sorry okay so now if we run agent two we will expect this to use the system prompt and remember this is system sorry this uh prompt and in this prompt remember that we were passing the context okay so this agent is aware of
[01:33:01] context okay so this agent is aware of the poem generated by the previous agent all right so this is the translation I don't know if you are experts in anian Greek but I am not so so yeah let's suppose uh this is okay um but in the final example I will be translating everything into everything into Spanish and since I am Spanish um I will be able to check uh if the translation is uh accurate okay so yeah let's get into the crew the crew remember that
[01:33:31] into the crew the crew remember that it's like our dck in airflow our orchestrator okay so let's get into vs code again and in this case we are going to uh to check the crew class okay the crew class has one argument for the ined method is a list of Agents and then it has a couple of of methods the enter method and the exit method that are used when defining
[01:34:02] exit method that are used when defining a session by the way if you are not aware of this session or context um what what I'm referring to when I speak about these Concepts is basically this okay this with as keywords that let you define a context where you are creating all the agents and all of these agents are going to be assigned with this context okay that's basically it and we need all of these uh fancy methods okay I'm not going to get into details here but you can check the code and you can modify whatever you want but yeah uh this enter and exit are for um entering
[01:34:34] this enter and exit are for um entering the context manager and exiting the context manager and then we have this current crew attribute that we need to keep track of the of the active crew context okay and we will need that uh because remember that uh on the agent we have this crew register agent so the agent if we call all this is going to be assigned to the active crew and that's something that we are going to to track by this current crew um class attribute okay that's basically I
[01:35:04] class attribute okay that's basically I know it's a bit tricky but I needed this uh for the interface of this framework to be um to be easy to to understand and to work with Okay apart from that we also have one method which is the add agent method that adds an agent to the agent list that we defined on the ended method method right right here we have this register agent that remember that uh this is the agent that we were calling from from the agent class that assigns or uh registers this
[01:35:37] class that assigns or uh registers this agent into the crew context manager and then uh this is the cool part maybe some of you uh have studied computer science or um know about basic data structures and algorithms Etc so this is a a very well-known uh algorithm for sorting graphs and I needed this um to decide the order in which the agents are going to be run okay so uh the agents are structured in um in a deck in a directed iy graph so I needed a way uh to sort
[01:36:09] iy graph so I needed a way uh to sort all of these agent okay and to run them without any Collision in the dependencies and that's basically what I'm doing here with this topological sord this is something that is going to be called whenever we are going to run a crew you will see that um later on the on the run method this plot is a very handy uh function that I will show you um in detail on on the Jupiter notebook uh we will see how to plot all the the the D all the graph that we have defining with the different agents and
[01:36:40] defining with the different agents and it's using graph Bas uh by the way and finally the run method it's going to apply as we were saying a topological sort and this topological sort is going to return a list of sorted agents this agent can be run with a for loop I mean we can iterate of this sorted agents list and we are not going to end up with any kind of problems with dependencies okay that's basically what we need this topological sword and we are going to use this colorama fancy
[01:37:12] are going to use this colorama fancy print function to make things uh much more easier to understand and to visualize and in jupyter Notebook and that's it so we will see the crew in action in jupter notebook right now all right so first of all let's import the crew uh class okay from multi-agent pattern crew import crew and now let's uh create the context the crew context with crew as crew and we are going to Define three agents in
[01:37:42] we are going to Define three agents in this case okay we are going to define the omnipresent uh poet agent then we are going to define a point translator agent which is a bit different than before because it's going to translate into Spanish as I was saying I'm Spanish so we will have the chance to understand and to check if this translation is is okay and finally we are going to get into the the third agent the writer agent which is going to take the Spanish poem and write it into a txt we are
[01:38:12] poem and write it into a txt we are going to use the tool that we defined and there you have it this beautiful graph based diagram so we have the poet agent the PO translator agent that depends on the poet agent and the writer agent that depends on the poem translator agent okay so you can see like all the now all the relationship in terms of context in terms of the output of one is the input for the for the other one and dependencies and dependence everything is condensed into
[01:38:42] dependence everything is condensed into this diagram at the end of the day okay and I think it's much uh simpler to see all of these dependencies with these diagrams than to getting into the vs code uh implementation and taking all the ad dependencies uh right shift left shift because it's a bit messy and and difficult to understand but if you have in mind that at the end of the day this um dck is exactly what we are trying to implement it makes things um much simpler Okay so we've just arrived to the last cell of the Jupiter notebook we
[01:39:12] the last cell of the Jupiter notebook we are going to run the crew and see how it goes so the first agent the PO agent is going to generate a poem in in English then the the translator agent is going to translate the poem into Spanish so let's check the English poem in the grand tapestry of time and space a threat of exist with dis place and yeah inish so yeah uh it's um an accurate an accurate translation and finally we are
[01:39:44] accurate translation and finally we are going to to see the last stent the writer agent that is going to use the writer string to text tool and write the the poem in Spanish into uh a file and the file is called poem txt okay so if we go to the Explorer yeah you will see the poem in Spanish written into the poem. txt so this is everything for today I hope you have enjoyed this uh
[01:40:14] today I hope you have enjoyed this uh lesson in my opinion this is the funniest lesson because we have uh put into practice all the patterns that we have learned in previous videos the react agent uh tools Etc and it's really cool to see how all these agents can collaborate and can interact with each other to to solve uh tasks of course this is not a very complex task I mean it's just a sequential um series of subtasks and they are I mean uh really simple we are just generating a poem
[01:40:44] simple we are just generating a poem translating the poem and writing the poem into into a file this is something that we could achieve with just one agent but I think that it um illustrates uh well how these multi-agent Frameworks like crui langra or autogen work uh under the hood so yeah I hope you have learned something new I hope uh you have uh enjoyed the video um as always if you like this content it will really uh support me if you uh click the like button or uh subscribe to the channel um
[01:41:17] button or uh subscribe to the channel um I would really appreciate that and yeah uh this is everything for today I hope I will see you in the next video and until then Happy coding [Music]
