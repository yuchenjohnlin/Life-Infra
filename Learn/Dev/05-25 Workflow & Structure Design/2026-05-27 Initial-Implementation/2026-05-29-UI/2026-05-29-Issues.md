%% When building the project manager agent, something to notice. 
As you can see we have two discussion files that talk about the same thing but because [[Learn/Dev/05-19 Summarize Skill Develop/Initial Implementation/Discussion|Discussion]] is the more cleaner and clear things whereas this note is kind of just the thoughts. and some of the issues that I found, so yes, I think I have actually thought about this before, which is to link this file to the Discussion file. 
%%
I went back and looked at [[input.base]] but the files inside had no thumbnails, and yes it reminded me that the new metadata and schema didn't include the thumbnail. I also found out that when I make changes in the older naming or something, then the base wouldn't update it automatically. No a big issue, but links already support this, I guess the filter in base might also be capable. 
Anyways, now create a base file for the files in Processed and Raw.

It is hard to recognize the video by just looking at the title, because when I watch the videos on youtube, I can see the thumbnail and that is a critical point.

Um, obviously the amount of files in the raw folder doesn't match that of the processed. And I think this is because I have been running a lot of tests and have already lost track. One reason is I like to move them to individual files and then there would be a lot of ran test in the dev folder, where none is left in the folder that actually runs it. This is because I don't have a good migration method. 