---
schema_version: youtube_raw_v1
source_type: youtube
source_url: ${source_url_yaml}
video_id: ${video_id_yaml}
title: ${title_yaml}
channel: ${channel_yaml}
channel_url: ${channel_url_yaml}
duration_seconds: ${duration_seconds}
upload_date: ${upload_date_yaml}
language: ${language}
thumbnail: ${thumbnail_yaml}
view_count: ${view_count}
like_count: ${like_count}
availability: ${availability}
live_status: ${live_status}
fetched_at: ${fetched_at_yaml}
manual_track_languages:
${manual_track_languages_yaml}
auto_track_languages:
${auto_track_languages_yaml}
transcript_status: ${transcript_status}
transcript_source: ${transcript_source}
transcript_target: ${transcript_target}
is_translated: ${is_translated}
has_description: ${has_description}
has_chapters: ${has_chapters}
chapter_count: ${chapter_count}
---

# ${title}

# Description

${description}

# Chapters

${chapters}

# Transcript

${transcript}
